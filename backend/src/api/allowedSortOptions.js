const ALLOWED_COLUMNS = ["section_id", "content_id", "content_name", "author", "section_name", "image"];
const ALLOWED_ORDER = ["asc", "desc"];


module.exports = {
    ALLOWED_COLUMNS: ALLOWED_COLUMNS,
    ALLOWED_ORDER: ALLOWED_ORDER
}