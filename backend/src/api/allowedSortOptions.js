const ALLOWED_SECTION_COLUMNS = ["section_id", "content_id", "content_name", "author", "section_name", "image"];
const ALLOWED_USER_COLUMNS = ["user_id", "user_name", "email", "image", "role"];
const ALLOWED_ORDER = ["asc", "desc"];


module.exports = {
    ALLOWED_SECTION_COLUMNS: ALLOWED_SECTION_COLUMNS,
    ALLOWED_ORDER: ALLOWED_ORDER,
    ALLOWED_USER_COLUMNS: ALLOWED_USER_COLUMNS
}